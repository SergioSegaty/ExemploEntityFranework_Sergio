
@{
    ViewData["Title"] = "Index";
}
@section Scripts{ 
    <script src="~/js/categoria/categoria-index.js"></script>
}

<h4 class="text-center mt-3 text-primary">Lista de Categorias</h4>

<button class="btn btn-primary mt-2 mb-2" data-target="#modal-categoria" data-toggle="modal">Nova Categoria</button>

<table class="table table-striped table-hover table-bordered"
       id="categoria-index">
    <thead>
        <tr>
            <th>Código</th>
            <th>Nome</th>
            <th>Ação</th>
        </tr>
    </thead>
</table>

<partial name="ModalCadastro.cshtml" />

